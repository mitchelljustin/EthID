<div class="row">
    <div class="col-sm-4 jumbotron">
        <p>
            These identities are stored and cryptographically secured in the Ethereum blockchain,
            and verified by EthID.
        </p>
        <p>
            If you want to link your own identity, visit <a href="http://ethid.xyz">ethid.xyz</a>.
        </p>
        <p>
            If you would like to use EthID in your Ethereum contract, visit <a href="/contracts">ethid.xyz/contracts</a>.
        </p>
    </div>
    <div class="col-sm-8">
        <h2 class="text-center">
            Identities for
            <br>
            <pre>{{ ethereumAddress }}</pre>

        </h2>
        {{#unless identities}}
            <h4>No verified identities for this address</h4>
        {{else}}
            <table class="table table-condensed">
                <thead>
                <tr>
                    <th>
                        Type
                    </th>
                    <th>
                        Email
                    </th>
                </tr>
                </thead>
                {{#each identities}}
                    <tr>
                        <td>
                            <a href="/contracts/{{ identityType }}">
                                {{ identityType }}
                            </a>
                        </td>
                        <td>
                            {{ emailAddress }}
                        </td>
                    </tr>
                {{/each}}
            </table>
        {{/unless}}

    </div>
</div>